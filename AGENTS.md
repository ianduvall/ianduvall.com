# repo guide

- use descriptive function and variable names instead of inline comments
- use pnpm instead of npm
- use pnpx instead of npx
- React 19 w/ the React Compiler enabled
- Next.js 16 w/ cache components enabled
- Tailwind 4
